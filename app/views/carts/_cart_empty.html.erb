<div class="col-span-full min-h-[50vh]">
  <h1 class="font-serif text-h4 mb-5 md:text-h3">
    <%= t('Votre panier est vide') %>
  </h1>

  <p class="text-body-sm mb-9 md:mb-14 md:text-body">
    Vous n'avez pas d'articles dans votre panier.
  </p>

  <%= render partial: "shared/call_to_action", :locals => { :label => t('Continuer le shopping'), :url => products_path } %>
</div>
